language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-rapid' ]; then cd .. && eval "mv $currentfolder generator-rapid" && cd generator-rapid; fi

