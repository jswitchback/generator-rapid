module.exports = {
  options: {
    enabled: true,
    max_jshint_notifications: 5, // maximum number of notifications from jshint output
    title: '<%= package.name %>' // defaults to the name in package.json, or will use project directory's name
  }
};
